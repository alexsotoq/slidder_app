name: slider_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0' # He actualizado esto a una versión más moderna de Dart SDK

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  flutter_dotenv: ^5.1.0
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0 # He actualizado esto a una versión más moderna

# The following section is specific to Flutter packages.
flutter:

  fonts:
    - family: PressStart2P
      fonts:
        - asset: assets/fonts/PressStart2P-Regular.ttf

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/maps/
    - assets/players/
    - assets/items/
    - assets/pokemon/
    - assets/pokemon/red/
    - assets/pokemon/leaf/
    - assets/audio/
    - assets/fonts/
    - assets/seamless-pokeball-pattern-vector-11290309.png
    - assets/pokemon/brendan/
    - assets/pokemon/may/
    - assets/enemies/biker/
    - assets/enemies/swimmers/
    - assets/pokemon/leafswim/
    - assets/pokemon/redswim/